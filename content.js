//
// I don't know how hot hell is, and I hope I never
// know, but I'm sure it isn't hot enough for those
// in the tech industry who are responsible for trying
// to destroy the United States of America.
// 
// DON'T MESS WITH TEXAS!  YOU'LL LIVE TO REGRET IT.
// 


/* const uniqueBy = (arr, prop) => {
    return [...new Map(arr.map((m) => [m[prop], m])).values()];
  };
  
  console.log(uniqueBy(_____a_href, 'href')); */


// remove duplicate value from array using set

//import 'GetLinks.js'

//import 'GetHref.js'

let LinksList = {};

let ___LinksArray = {};

function GetLinks(LinksArray) {

    //let linksArray = new Array.constructor;

    let linksList = {}; 

    let linksArray = {};  //new Array.constructor;

    linksArray = document.all[0].getElementsByTagName('a');

    LinksArray = linksArray;

    for (let index = 0; index < linksArray.length; index++) {

        linksList[index] = linksArray[index].href;

    }

    LinksList = linksList;

    return LinksArray;
}

let ___al = GetLinks(___LinksArray);

let ___fullInnerText = document.all[0].innerText;

let ___fullOuterText = document.all[0].innerText;

let ___HrefArray = {};

function GetHref(HrefArray, LinksArray) { 
    
    let linksArray = LinksArray;

    for (let index = 0; index < linksArray.length; index++) {
        HrefArray[index] = linksArray[index].href
    }
    
    return HrefArray;

}

GetHref(___HrefArray, ___al);

/*
(function(blueConic, undefined) {
    var ps = 'https://www.www.www/';
    var pf = '/www/';
 }
);
*/














function removeDuplicates(arr) {

    // removing duplicates
    let uniqueArr = [...new Set(arr)];

    console.log(uniqueArr);
};


function getMyLinks(arg, arg1) {

    for(i=0; i < arg1.length; i++) {

    //[266].childNodes[0].ownerDocument.location

const uniqueBy = (arr, prop) => {
    return [...new Map(arr.map((m) => [m[prop], m])).values()];
};
      
            //console.log(uniqueBy(_____a_href, 'href'));

            arg[i] = arg1[i].outerHTML.valueOf();
    
            // this works, but trying something else
            //console.log(arg1[i].outerHTML.valueOf())
            console.log(arg1[i].href);
    
            // arg[i] = JSON.stringify(arg1[i].childNodes[0].ownerDocument.outerHTML)
    
        }

    return arg;

}

// const array = [1, 2, 3, 2, 3];

// remove duplicates from array:
// removeDuplicates(array);


// (new MutationObserver(check)).observe(document, {childList: true, subtree: true});
// 
// function check(changes, observer) {
//     if(document.querySelector('#mySelector')) {
//         observer.disconnect();
//         // code
//     }
// }


(new MutationObserver(check)).observe(document, {childList: true, subtree: true});

function check(changes, observer) {
    if(document.querySelector('html')) {
    
        observer.disconnect();
        
        console.log("This is a Google Chrome Extension by Shadow.Cipher");
        console.log("");


        let _____a_href = Array.from(document.getElementsByTagName('a'));

        //'this.document.getElementsByTagName('a')[0].childNodes[0].parentElement.outerHTML

        // let _____a_href = new Array;

        // _____a_href = Array.from(document.getElementsByTagName('a'));

        let _____a_Links = [];

// code was here

        getMyLinks(_____a_Links, _____a_href);


        ////let ______aInnerText = document.getElementsByTagName('body')[0].innerText;
        let ______aInnerHTML = document.getElementsByTagName('body')[0].innerHTML;
        let ______aOuterHTML = document.getElementsByTagName('body')[0].outerHTML;
        
    }
}


function check(changes, observer) {
    if(document.querySelector('#clarify-box')) {
    
        observer.disconnect();
        
        var YT_ClearStupidBullshitPropaganda = document.getElementById('clarify-box');
        
        YT_ClearStupidBullshitPropaganda.remove();
        
    }
}




        console.log("This is a Google Chrome Extension by Shadow.Cipher");
        console.log("");


    function removeDuplicates(arr) {
        return [...new Set(arr)];
    }


        let _____a_href = Array.from(document.getElementsByTagName('a'));

        //'this.document.getElementsByTagName('a')[0].childNodes[0].parentElement.outerHTML

        // let _____a_href = new Array;

        // _____a_href = Array.from(document.getElementsByTagName('a'));

        let _____a_Links = [];

        // function getMyLinks(arg, arg1) {

        //     for(i=0; i < arg1.length; i++) {
    
        //     //[266].childNodes[0].ownerDocument.location
    
        //             arg[i] = arg1[i].outerHTML.valueOf();
            
        //             // this works, but trying something else
        //             //console.log(arg1[i].outerHTML.valueOf())
        //             console.log(arg1[i].href);
            
        //             // arg[i] = JSON.stringify(arg1[i].childNodes[0].ownerDocument.outerHTML)
            
        //         }
        
        //     return arg;
    
        // }

        getMyLinks(_____a_Links, _____a_href);

        _____a_Links = removeDuplicates(_____a_Links);


        ////let ______aInnerText = document.getElementsByTagName('body')[0].innerText;
        let ______aInnerHTML = document.getElementsByTagName('body')[0].innerHTML;
        let ______aOuterHTML = document.getElementsByTagName('body')[0].outerHTML;


console.log(document.all[0].outerText);

console.log(document.all[0].innerText);

var HDN = "";


var newScript = document.createElement('script');
newScript.type = 'text/javascript';
newScript.src = './TagBossHTML.js';
//document.getElementsByTagName('head')[0].appendChild(newScript);

//remove the right hand column on Facebook:
//document.all[0].getElementsByTagName('span').ssrb_rhc_start.parentElement.remove();


import 'ingestion.js';
